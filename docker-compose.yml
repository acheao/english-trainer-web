services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:8080}
    container_name: english-trainer-web
    ports:
      - "${WEB_PORT:-8088}:80"
    restart: unless-stopped
